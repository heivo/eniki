
spring:
  profiles:
    active: dev
  data:
    rest:
      base-path: /api

server:
  port: 8081


#security:
#  oauth2:
#    client:
#      client-id: acme
#      client-secret: acmesecret
#      scope: read,write
#      auto-approve-scopes: '.*'

logging:
  level:
    org.springframework.security: DEBUG